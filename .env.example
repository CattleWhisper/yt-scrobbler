# Plex Webhook Configuration
WEBHOOK_PORT=5000

# Plex Server Connection (Required)
# The scrobbler connects to your Plex server to fetch media file paths
# because webhooks don't include this information
PLEX_URL=http://localhost:32400
PLEX_TOKEN=your_plex_token_here

# How to get your Plex Token:
# 1. Open a media item in Plex Web App
# 2. Click "..." menu → "Get Info" → "View XML"
# 3. Look at the URL - the token is after "X-Plex-Token="
# Or follow: https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/

# YouTube Authentication
# Use cookies file (RECOMMENDED - simpler setup)
# Export your YouTube cookies using a browser extension like:
# - "Get cookies.txt LOCALLY" for Firefox/Chrome
# - Or use yt-dlp: yt-dlp --cookies-from-browser firefox --cookies cookies.txt
YOUTUBE_COOKIES_FILE=cookies.txt

# Optional: Filter to only track specific Plex libraries
# PLEX_LIBRARY_FILTER=YouTube Videos,YouTube

# Minimum watch percentage to consider as "watched" (default: 90%)
MIN_WATCH_PERCENTAGE=90
